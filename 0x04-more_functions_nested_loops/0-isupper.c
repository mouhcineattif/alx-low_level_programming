#include <stdio.h>

int is_uppercase(int c);

int main() {
	    int result1 = is_uppercase('W');
	        int result2 = is_uppercase('a');
		    
		    printf("%d\n", result1);  // Output number 1 which we have it already the "W"
					      //     printf("%d\n", result2);  // Output number 2 which we have it already the "a"
					      //         
					      //             return 0;
					      //             }
					      //
					      //             int is_uppercase(int c) {
					      //                 if (c >= 'A' && c <= 'Z') //this is the rang of the alphabethics  
					      //                     {
					      //                             return 10;
					      //                                 } else {
					      //                                         return 1;
					      //                                             }
					      //                                             }
					      //                                             //end of the programme
